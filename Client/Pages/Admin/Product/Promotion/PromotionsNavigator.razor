@page "/admin/promotions"

@code {
	[Inject]
	NavigationManager navigationManager { get; set; }
	protected override void OnInitialized()
	{
		navigationManager.NavigateTo("/admin/promotions/page/1");
	}
}
